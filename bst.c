#include<stdio.h>
#include<stdlib.h>

struct node{
    int data;
    struct node *left;
    struct node *right;
};

struct node *root,*current;

void create()
{
    int num;
    printf("\nEnter the initial numbers of nodes:");
    scanf("%d",&num);
    int i=0;
    while (i<num)
    {
    insert()
    }
    
}